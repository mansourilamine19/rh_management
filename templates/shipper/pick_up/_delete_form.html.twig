<form method="post" action="{{ path('app_pick_up_delete', {'id': pick_up.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pick_up.id) }}">
    <button class="btn">Delete</button>
</form>
